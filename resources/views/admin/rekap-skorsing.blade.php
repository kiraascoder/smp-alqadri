@extends('components.admin')
@section('title', 'Rekap Skorsing')
@section('content')
<div class="py-8 space-y-6"><div><h1 class="text-3xl font-bold">Rekap Skorsing</h1><p class="text-gray-500">Seluruh riwayat skorsing dari Admin dan Guru.</p></div>
<div class="bg-white rounded-2xl shadow p-6"><form method="GET" class="grid grid-cols-1 md:grid-cols-3 xl:grid-cols-6 gap-3"><input type="date" name="tanggal_mulai" value="{{ request('tanggal_mulai') }}" class="border rounded-xl px-3 py-2"><input type="date" name="tanggal_selesai" value="{{ request('tanggal_selesai') }}" class="border rounded-xl px-3 py-2"><select name="pembuat_id" class="border rounded-xl px-3 py-2"><option value="">Semua pembuat</option>@foreach($pembuatList as $u)<option value="{{ $u->id }}" @selected(request('pembuat_id')==$u->id)>{{ $u->name }}</option>@endforeach</select><select name="kelas_id" class="border rounded-xl px-3 py-2"><option value="">Semua kelas</option>@foreach($kelasList as $k)<option value="{{ $k->id }}" @selected(request('kelas_id')==$k->id)>{{ $k->nama_kelas }}</option>@endforeach</select><select name="siswa_id" class="border rounded-xl px-3 py-2"><option value="">Semua siswa</option>@foreach($siswaList as $s)<option value="{{ $s->id }}" @selected(request('siswa_id')==$s->id)>{{ $s->nama }}</option>@endforeach</select><select name="pelanggaran_id" class="border rounded-xl px-3 py-2"><option value="">Semua pelanggaran</option>@foreach($pelanggaranList as $p)<option value="{{ $p->id }}" @selected(request('pelanggaran_id')==$p->id)>{{ $p->deskripsi }}</option>@endforeach</select><button class="xl:col-span-5 bg-blue-700 text-white rounded-xl py-2">Terapkan Filter</button><a href="{{ route('admin.rekap-skorsing') }}" class="text-center border rounded-xl py-2">Reset</a></form></div>
<div class="bg-white rounded-2xl shadow overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-gray-50"><tr><th class="p-4 text-left">Tanggal</th><th class="p-4 text-left">Siswa</th><th class="p-4 text-left">Kelas</th><th class="p-4 text-left">Pelanggaran</th><th class="p-4 text-left">Skor</th><th class="p-4 text-left">Dibuat Oleh</th><th class="p-4 text-left">Keterangan</th></tr></thead><tbody>@forelse($rekap as $item)<tr class="border-t"><td class="p-4">{{ $item->tanggal?->format('d/m/Y') }}</td><td class="p-4 font-medium">{{ $item->siswa?->nama }}</td><td class="p-4">{{ $item->siswa?->kelas?->nama_kelas }}</td><td class="p-4">{{ $item->pelanggaran?->deskripsi }}</td><td class="p-4">{{ $item->skor }}</td><td class="p-4">{{ $item->creator?->name ?? 'User dihapus' }}</td><td class="p-4">{{ $item->keterangan ?? '-' }}</td></tr>@empty<tr><td colspan="7" class="p-8 text-center text-gray-500">Tidak ada data.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $rekap->links() }}</div></div></div>
@endsection
